import { MoviePageProps } from "../types/types";
import { RelatedMoviesProps } from "../types/types";

function RelatedMovies({ movieId, type }: RelatedMoviesProps) {
  return <></>;
}

export default RelatedMovies;
